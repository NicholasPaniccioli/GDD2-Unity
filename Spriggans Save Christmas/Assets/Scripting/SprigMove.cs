using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Started by Kyle Mulvey
// Beginning to implement the generation of the grid.

public class SprigMove : MonoBehaviour
{
    int x = 0;  // x coordinate
    int y = 0;  // y coordinate
    int xSize = 10; // number of squares in the x direction
    int ySize = 10; // number of squares in the y direction

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
